/** @type {import('next-sitemap').IConfig} */
module.exports = {
  // Replace with the actual production URL once confirmed (e.g., https://jarwix.com)
  siteUrl: process.env.NEXT_PUBLIC_SITE_URL || 'https://jarwix-agency.com',
  generateRobotsTxt: true, // (optional)
  // ...other options
};
